# Static Files Directory

Place the PIGGY logo image here as `logo.png`.

The logo should be the PIGGY logo with the piggy bank icon as shown in the design.

Place the banknote image here as `banknote.png`.

The banknote image will be used as the background for the "Total Income", "Total Expense", and "Balance" stat boxes.

